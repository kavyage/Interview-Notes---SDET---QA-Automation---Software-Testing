String str= "W I_P-R%O"
  String lettersOnly = str.replaceAll("[^a-zA-Z]", "");

[or]  for (int i = 0; i < str.length(); i++) {
            char ch = str.charAt(i);
            if (Character.isLetter(ch)) {
                sb.append(ch);
            }
        }
