set chromium browser
launch and open new tab
write python script to print all request and response of website
use no function to print request and response


from selenium import webdriver
from selenium.webdriver.chrome.service import Service
from selenium.webdriver.chrome.options import Options
from selenium.webdriver.common.desired_capabilities import DesiredCapabilities

# Enable logging of network requests
caps = DesiredCapabilities.CHROME
caps['goog:loggingPrefs'] = {'performance': 'ALL'}

# Set up Chromium browser
options = Options()
options.add_argument("--auto-open-devtools-for-tabs")  # optional, opens devtools

driver = webdriver.Chrome(desired_capabilities=caps, options=options)

# Open website in new tab
driver.get("https://example.com")

# Print all network requests and responses
logs = driver.get_log("performance")
for log in logs:
    message = log['message']
    print(message)

driver.quit()
